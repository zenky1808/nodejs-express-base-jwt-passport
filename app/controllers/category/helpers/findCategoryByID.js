
const { buildErrObject } = require('../../../middleware/utils')

const findCategoryByID = (id, model) => {
    return new Promise((resolve, reject) => {

    })
}

module.exports = { findCategoryByID }